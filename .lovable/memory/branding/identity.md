# Memory: branding/identity
Updated: now

The project brand is 'ChatSoap' (rebranded from Messem, originally Silicon Valhalla). Invitation codes use the 'SOAP-XXXX' format. Favicon is `/chatsoap-icon.png`, OG image is `/og-image.png`.
